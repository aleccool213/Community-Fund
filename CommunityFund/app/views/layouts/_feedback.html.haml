- if user_signed_in?
  - @feedbacks = Feedback.not_dismissed.where(user_id: current_user.id)
  - if @feedbacks.count >= 1
    .row
      - @feedbacks.each do |feedback|
        - @feedback = feedback

        = render partial: "feedbacks/feedback"